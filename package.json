{
  "name": "minecraft-websocket-server",
  "version": "1.0.0",
  "description": "A WebSocket server for wireless communication with Minecraft CC:Tweaked computers",
  "author": "StinkyHead",
  "scripts": {
    "bundle": "esbuild src/index.js --bundle --platform=node --target=node22 --format=cjs --outfile=dist/bundle.cjs",
    "sea": "node --experimental-sea --experimental-sea-config sea-config.json"
  },
  "dependencies": {
    "ws": "^8.19.0"
  }
}
